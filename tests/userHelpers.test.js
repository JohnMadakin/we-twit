import { expect } from 'chai';

import userHelpers from '../server/helpers/userHelpers';

// test('userHelpers class', () => {
//   it('should generate a valid hashed password', () => {
//     const pass = 'password';
//     const hashedpassword = userHelpers.hashPassword(pass);
//     expect(hashedpassword).toEqual('string');
    
//   });
// });

it('adds 1 + 2 to equal 3', () => {
  expect(3).to.be.equal(3);
});
